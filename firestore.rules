rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Default-deny catch-all safety net
    match /{document=**} {
      allow read, write: if false;
    }

    // Users Collection rules: Users can read and write only their own profiles.
    // Permanent deletion of admin overrides.
    match /users/{userId} {
      allow read: if isSignedIn() && request.auth.uid == userId;
      allow create, update: if isSignedIn() && request.auth.uid == userId && isValidUser(request.resource.data);
      allow delete: if false;

      match /withdrawals/{withdrawalId} {
        allow read, write: if isSignedIn() && userId == request.auth.uid;
      }
    }

    // Deposits Collection rules: Relational checks against resource data
    match /deposits/{depositId} {
      allow read: if isSignedIn() && resource.data.userId == request.auth.uid;
      allow create: if isSignedIn() && request.resource.data.userId == request.auth.uid && isValidDepositRecord(request.resource.data);
      allow update: if isSignedIn() && resource.data.userId == request.auth.uid && isValidDepositRecord(request.resource.data);
      allow delete: if false;
    }

    // Withdrawals Collection rules: Secure queries validation
    match /withdrawals/{withdrawalId} {
      allow read: if isSignedIn() && resource.data.userId == request.auth.uid;
      allow create: if isSignedIn() && request.resource.data.userId == request.auth.uid && isValidWithdrawalRecord(request.resource.data);
      allow update: if isSignedIn() && resource.data.userId == request.auth.uid && isValidWithdrawalRecord(request.resource.data);
      allow delete: if false;
    }

    // Transactions Collection rules: every financial action log
    match /transactions/{transactionId} {
      allow read: if isSignedIn() && resource.data.userId == request.auth.uid;
      allow create: if isSignedIn() && request.resource.data.userId == request.auth.uid && isValidTransactionRecord(request.resource.data);
      allow update: if isSignedIn() && resource.data.userId == request.auth.uid && isValidTransactionRecord(request.resource.data);
      allow delete: if false;
    }

    // Dynamic Plans Collection rules: Read-only for everyone, disabled admin editing/creation
    match /plans/{planId} {
      allow read: if true;
      allow write, delete: if false;
    }

    // Global Settings matches: Read-only for everyone, disabled admin editing
    match /settings/{settingsId} {
      allow read: if true;
      allow write: if false;
    }

    // Helper functions
    function isSignedIn() {
      return request.auth != null;
    }

    function isAdmin() {
      return false;
    }

    function isValidSettings(data) {
      return data.keys().hasAll(['id'])
             && data.id is string
             && data.id.size() <= 128;
    }

    function isValidPlan(data) {
      return data.keys().hasAll(['id', 'name', 'min', 'max', 'roi', 'term'])
             && data.id is string
             && data.id.size() <= 128
             && data.name is string
             && data.name.size() <= 100
             && data.min is number
             && data.max is number
             && data.roi is number
             && data.term is number;
    }

    function isValidUser(data) {
      return data.keys().hasAll(['uid', 'username', 'fullName', 'email'])
             && data.uid is string
             && data.uid.size() <= 128
             && data.username is string 
             && data.username.size() <= 128
             && data.fullName is string 
             && data.fullName.size() <= 128
             && data.email is string 
             && data.email.size() <= 128;
    }

    function isValidDepositRecord(data) {
      return data.keys().hasAll(['id', 'userId', 'username', 'amount', 'date', 'processor'])
             && data.id is string
             && data.id.size() <= 128
             && data.userId is string
             && data.userId.size() <= 128
             && data.username is string 
             && data.username.size() <= 100
             && data.amount is number
             && data.date is string
             && data.processor is string;
    }

    function isValidWithdrawalRecord(data) {
      return data.keys().hasAll(['id', 'userId', 'username', 'amount', 'date', 'processor'])
             && data.id is string
             && data.id.size() <= 128
             && data.userId is string
             && data.userId.size() <= 128
             && data.username is string 
             && data.username.size() <= 100
             && data.amount is number
             && data.date is string
             && data.processor is string;
    }

    function isValidTransactionRecord(data) {
      return data.keys().hasAll(['id', 'userId', 'username', 'type', 'amount', 'date', 'timestamp', 'status', 'processor'])
             && data.id is string
             && data.id.size() <= 128
             && data.userId is string
             && data.userId.size() <= 128
             && data.username is string 
             && data.username.size() <= 100
             && data.type is string
             && data.amount is number
             && data.date is string
             && data.timestamp is number
             && data.status is string
             && data.processor is string;
    }
  }
}
